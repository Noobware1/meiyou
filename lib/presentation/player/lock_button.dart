import 'package:flutter/material.dart';

class PlayerLockButton extends StatelessWidget {
  const PlayerLockButton({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
        onPressed: () {}, icon: const Icon(Icons.lock_outline_rounded));
  }
}
